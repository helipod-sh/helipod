# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

**stackbase** — an open-source, self-hostable Backend-as-a-Service in the spirit of [Convex](https://github.com/get-convex/convex-backend). The goal: write TypeScript query/mutation functions, have them run server-side and transactionally, and get **reactive** results — when underlying data changes, subscribed clients are pushed updates over a WebSocket. Convex-grade DX, easy self-hosting, no vendor lock-in.

It is loosely inspired by a now-defunct project, "concave.dev." There is **no recoverable concave source or published package** (the original author's npm/GitHub are gone, and nothing is findable). So this is **not a literal fork** — it is a clean-room build using Convex's open, well-documented architecture as the reference. Do not claim to be reverse-engineering concave; there is no artifact to reverse-engineer.

## Status: Foundation slice BUILT (M0–M11 ✅)

The reactive engine is implemented and working end-to-end: 14 packages under `packages/`, a runnable example under `examples/chat`, **131 passing tests**, build/typecheck green, on both Node and Bun. `bun install`, `bun run build`, `bun run test`, `bun run typecheck` all work (Bun is the package manager + runtime; Turborepo orchestrates; vitest runs under Bun). The commands and package layout below are now **real**, not aspirational.

What works: MVCC SQLite storage · single-writer OCC transactor · query engine with cursor pagination · isolate-safe syscall executor · reactive sync tier (subscribe→write→push) · embedded runtime + loopback/WebSocket transports · codegen (typed `Doc`/`Id`/`api` that compiles) · `stackbase dev` CLI (HTTP + hot reload) · client SDK with `useQuery`/`useMutation` · the `examples/chat` app (reactive, shard-key, pagination, ephemeral typing) · **dashboard** (`apps/dashboard`) with a **live** data browser (admin sync subscriptions — `_admin:browseTable`, cursor pagination, structured filter UI, scanCapped banner), logs viewer, and function runner · **`@stackbase/scheduler`** (component, opt-in per project via `stackbase.config.ts` — there is no auto-install/`init` command; `examples/auth-demo/stackbase.config.ts` shows the reference pattern — Convex-parity `ctx.scheduler.runAfter`/`runAt`/`cancel`, `cronJobs()` recurring/cron schedules with catch-up policies, retries/backoff, cascading cancel, and workflow-ready `onComplete`/`context` round-trip primitives), built on a new **recurring `driver` component seam** (`ComponentDefinition.driver`/`boot` — a reactive event loop woken by the commit fan-out plus a wall-clock timer, wired into `packages/component`'s `composeComponents` and `packages/runtime-embedded`'s `EmbeddedRuntime`/`DriverContext`; `packages/cli`'s `devCommand` now passes the composed `bootSteps`/`drivers` through to `createEmbeddedRuntime`, which previously silently dropped them) · **`@stackbase/workflow`** (component, opt-in per project via `stackbase.config.ts`, `requires: ["scheduler"]` — built ON `@stackbase/scheduler`, not a replacement for it; `examples/auth-demo/stackbase.config.ts` shows the reference pattern composed alongside `defineScheduler()` — durable multi-step workflows via deterministic replay over a durable `workflows`/`steps`/`events` journal, with a `generationNumber` OCC guard against double-advance/stale-cancel races; Convex `@convex-dev/workflow`-parity authoring surface: `workflow.define({handler})`, `step.runMutation`/`runQuery`/`runAction`/`sleep`/`sleepUntil`, `ctx.workflow.start`/`cancel`, a LIVE `workflow:status` query, and workflow-level `onComplete`/`context` round-trip; `step.waitForEvent`/`ctx.workflow.sendEvent` for durable external signals — no scheduler job, parks on an `events` row until resolved; `Promise.all([step.a(), step.b()])` fan-out/fan-in with a `maxParallelism` dispatch cap; `ctx.workflow` works from actions too (`workflowActionContext`, delegating to internal `workflow:_start`/`_cancel`/`_sendEvent` mutations — the same action-mode-facade-delegates-to-an-internal-mutation pattern `@stackbase/scheduler`'s `schedulerActionContext` established), proven end-to-end through the real `stackbase dev` server in `packages/cli/test/workflow-e2e.test.ts`. **Saga/compensation has since SHIPPED too**: per-step `{ compensate: FnRef }` handlers recorded on the journal (`compensateFnPath`), a reverse-`stepNumber`-order unwind (`workflow:_compensate`/`_compensateDone`) triggered on handler failure AND on `ctx.workflow.cancel`ing a `"running"` workflow (compensate-by-default, opt out with `{ compensate: false }`), halt-on-failed-compensation (a compensation that itself exhausts retries stops the walk and terminal-fails with the halt reason alongside the original error, rather than silently continuing or hanging), a step's own `{ maxAttempts }` also capping its compensation's retries, and cascade-cancel of an in-flight compensation job — proven end-to-end through the real `stackbase dev` server in `packages/cli/test/workflow-saga-e2e.test.ts` (`running` -> `compensating` -> `failed`, with the compensation's own row effect visible via admin browse). Sub-workflows, replay-debugging, and handler versioning are deferred to v1.1 — not built.) · **actions** (`action` functions execute *outside* the transaction — no `ctx.db`, native `fetch`/`Date`/`Math.random`/timers, orchestrating data access via `ctx.runQuery`/`ctx.runMutation`/`ctx.runAction`, each a fresh independent top-level run; callable from the scheduler as at-most-once scheduled actions (including crons), from other functions via `ctx.runAction`, from the client via `client.action()`/`useAction` over the WebSocket sync connection, and via HTTP `POST /api/run` with an action path — proven end-to-end through the real `stackbase dev` server in `packages/cli/test/action-e2e.test.ts`, including a client `Action` message whose inner `ctx.runMutation` write fans out reactively to a separate live subscription) · **`httpAction` + the public HTTP router** (`httpAction` is an action variant whose I/O is a raw `Request` -> `Response` instead of JSON args -> value, same non-deterministic context as `action` — `runQuery`/`runMutation`/`runAction`, native `fetch`/clock, no `ctx.db`; a conventional `http.ts` default-exports an `httpRouter()` of exact-`path`/`pathPrefix` routes at bare Convex-parity paths, method + path matched with exact-path precedence over longest-prefix; `/api/*` and any `/_*` path are reserved for the engine and rejected by `route()` at registration time — before a malformed `http.ts` can ever shadow a built-in; the caller's raw `Bearer <token>` is passed straight through as `opts.identity` — no resolution); webhook -> `ctx.runMutation` -> reactive fan-out proven end-to-end through the real `stackbase dev` server in `packages/cli/test/http-action-e2e.test.ts`, including the reload wiring (`server.setRoutes(...)`, the same call `stackbase dev`'s hot-reload watch loop makes after re-resolving `http.ts`) and the reserved-path registration guard. Streaming request/response bodies, automatic CORS, named path params, and per-route middleware are non-goals — not planned, not deferred) · **`stackbase serve` + Docker self-host** (slice 6a — the production entrypoint: requires `STACKBASE_ADMIN_KEY` (fails fast if unset), binds `0.0.0.0`, never runs codegen at boot (fails fast if `convex/_generated/` is missing — codegen and commit it beforehand), graceful shutdown on `SIGTERM`/`SIGINT`, serves sync + HTTP + `httpAction`s + dashboard; shares the `bootProject` boot core with `dev` so the two entrypoints can't drift; unlike `dev`, the dashboard is served key-less (`loadDashboard(undefined)`) so the admin key is never embedded in HTML, and the SPA prompts for it. A working `docker compose up`: the repo `Dockerfile`'s `runner` stage runs `serve` as its entrypoint, `docker-compose.yml` builds that stage, bind-mounts an app's `convex/` read-only into the generic image, and persists SQLite on a named `stackbase-data` volume — see `docs/enduser/self-hosting.md` for the guide, including the bake-into-image alternative for immutable deploys and the reverse-proxy/TLS note. **Both deploy paths are smoke-verified end-to-end against a real container (2025-07-19): (1) bind-mount `docker compose up`, and (2) the immutable `FROM stackbase:latest` + `COPY ./convex /app/convex` wrapper — each confirmed for build → boot → `/api/health` → mutation-commit via `POST /api/run` → read-back → persistence across container recreate on the volume → dashboard served key-less.** That smoke caught two container-only bugs the host-based tests structurally cannot (both fixed in the `runner` stage, both would otherwise crash-loop on the FIRST `docker compose up`): (a) the runner stage must symlink every workspace `@stackbase/*` package into `/app/node_modules/@stackbase/<name>` — turbo-prune keeps those links nested per-package, never at the root, but a mounted/baked `/app/convex` resolves its bare `import "@stackbase/values"` (every `schema.ts`) and `_generated/server` re-exports UP to `/app/node_modules`; and (b) `/data` must be `chown`ed to the non-root `bun` user before `USER bun`, because a fresh named volume inherits the image dir's root ownership and uid-1000 otherwise can't create `/data/db.sqlite` (EACCES). Guarded by text assertions in `packages/cli/test/docker-config.test.ts`; the lesson — a review's "manual-smoke risk" is unconfirmed until the artifact is actually smoked.) · **`stackbase deploy` push-based live deploy** (slice 6b — hot-swap functions + additive schema onto an already-running `serve`, no restart: `POST /_admin/deploy` is opt-in per deployment via `serve --allow-deploy`/`STACKBASE_ALLOW_DEPLOY=1` (off by default — falls through to the generic admin-router 404, not a deploy-shaped response, when disabled), authenticated with the same `STACKBASE_ADMIN_KEY` bearer token as the dashboard (401 on a wrong key, checked before anything is applied); `stackbase deploy --url <url> --dir convex` transpiles the local `convex/` tree with esbuild `transform` (types stripped, import specifiers untouched — bare `@stackbase/*` resolves against the target's own `node_modules`) and POSTs the file tree; the server writes it under a per-revision dir, `loadConvexDir -> push`s it against the boot-time component set, then gates the swap on `diffSchema` — an **additive-only** schema rule (new tables and new *optional* fields on existing tables are accepted; a dropped/renamed table, a changed table number, a removed field, a field type change, or a new required field are all rejected, no migrations) — and only after both load and diff succeed does it **atomically** swap `runtime.setModules`/`setTableNumbers`, `setRoutes`, and the admin API's live schema, so a rejected deploy never leaves the server mid-swap. The component set itself (e.g. `@stackbase/scheduler`/`@stackbase/workflow`) is fixed at boot — adding/removing components needs a restart, only functions/schema hot-swap. Proven end-to-end through the real `stackbase serve` server in `packages/cli/test/deploy-e2e.test.ts`: a v1→v2 live deploy makes a brand-new mutation (`notes:add`) immediately callable AND its write fans out reactively to a WebSocket subscription opened *before* the deploy, a destructive deploy is rejected while v2 stays fully live, a second server without `--allow-deploy` rejects the CLI with the "not enabled" message, and a wrong admin key against an allow-deploy server 401s. See `docs/enduser/deploying.md`.) · **`stackbase build` single-binary compile** (compiles an app's `convex/` + composed components into one self-contained executable via `bun build --compile`: codegens the app fresh, then codegens a static-import entrypoint — `bun build --compile` only bundles static imports, so the entrypoint statically imports every module/schema/`stackbase.config.ts`/dashboard asset instead of the dir-scan `stackbase dev` uses — and shells out to `bun build --compile --minify` (no `--bytecode`: the entry's top-level `await` is rejected by `--bytecode`, and cold-start speed doesn't matter for a long-running server). Embeds the Bun runtime, the engine, `bun:sqlite`, the app's functions/schema, any composed components, and the dashboard (default on, `--no-dashboard` to exclude); the SQLite database file itself stays external, under `--data-dir`. Cross-compiles via `--target linux-x64|linux-arm64|darwin-x64|darwin-arm64|windows-x64`. The produced binary is runtime-flag-compatible with `stackbase serve` (`--port`/`--hostname`/`--data-dir`, required `STACKBASE_ADMIN_KEY` fail-fast, graceful `SIGTERM`/`SIGINT` shutdown) and prints the same machine-readable `{"ready":true,"port":N,"url":"http://<host>:<port>"}` stdout line for parent-process integration (Electron/Tauri sidecars). Proven end-to-end through a real compiled binary in `packages/cli/test/build-e2e.test.ts`: a fixture app with a composed component compiles, boots, serves a committing mutation via `POST /api/run`, and cross-compiles to `linux-x64`. Unlike `stackbase serve --allow-deploy`, the binary has no live hot-swap path — functions, schema, and the component set are fixed at build time; see `docs/enduser/deploy/standalone-binary.md`.) · **Postgres storage adapter** (slice 6c — `@stackbase/docstore-postgres`: `PostgresDocStore` sits on a narrow async `PgClient` seam (`query`/`transaction`/`acquireWriterLock`/`close`) analogous to `docstore-sqlite`'s synchronous `DatabaseAdapter`, so the engine never imports a driver directly; the shipped implementation is the `pg` driver (`node-pg-client.ts`), with `Bun.SQL` reserved as a seam-swappable fast-follow rather than built now. Physically schemaless — documents live in a small fixed set of internal tables (the same MVCC-log shape SQLite uses), so app tables/fields are data, not DDL, and no migration step is ever needed as `schema.ts` evolves. Reads use set-based `DISTINCT ON`/`LATERAL` scans rather than per-row round trips. A `pg_advisory_lock` (`acquireWriterLock`) enforces the single-writer invariant — a second engine against the same database fails fast instead of corrupting state. Selected via `stackbase serve --database-url postgres://…` or `STACKBASE_DATABASE_URL` (flag wins; unset → SQLite default, no code changes needed either way). Proven by the shared docstore conformance suite run against both SQLite and PGlite (in-process Postgres) for behavioral parity, plus a real-container Docker E2E (`postgres:16`) through the actual `stackbase serve` entrypoint. See `docs/enduser/self-hosting.md`'s "Using Postgres" section.) · **file storage** (slice 4 — `@stackbase/storage`: a reserved app-root `_storage` system table (`Id<"_storage">` is a first-class typed reference, usable in `schema.ts` like any other id, and participates in reactivity the same as every other table) plus `ctx.storage`, wired as an always-on context provider (not opt-in via `stackbase.config.ts` — every project gets it). `ctx.storage.generateUploadUrl({contentType?, visibility?})` (mutation-only, deterministic — the blob key is the new row's own id, `expiresAt`/token `exp` derive from `cctx.now`, never wall-clock) mints a pending row plus an `UploadTarget`; two upload shapes share one client contract: **proxied** (FS backend — the client `POST`s bytes straight to our own `/api/storage/upload` capability-token-gated endpoint, which stores + finalizes in one round trip) and **presigned** (S3 backend — the client `PUT`s bytes directly to the bucket, bypassing the server entirely, then `POST`s the returned `confirmUrl` to flip the row to `ready`). `getUrl`/`getMetadata`/`delete` are metadata-only and work from queries/mutations/actions alike; `store`/`get` do actual byte I/O and are action-only (same "no non-determinism in a transaction" rule as `fetch`/`Date.now`). Byte storage itself sits behind a narrow `BlobStore` seam — `@stackbase/blobstore-fs` (zero-config default, `<data-dir>/storage`) and `@stackbase/blobstore-s3` (any S3-compatible bucket: AWS S3, MinIO, R2), selected purely by whether `STACKBASE_STORAGE_BUCKET`/`--storage-bucket` is set (unset → FS; the engine never imports a driver directly, same story as `DocStore`). Files are **private by default**: `getUrl` on a private file embeds an HMAC capability token (signed with the deployment admin key) in the URL, verified by the serve endpoint's no-authz fallback — bearer semantics, like Convex, with no per-user/per-role check yet (a `checkRead` authz seam is threaded through `packages/storage/src/http.ts` but intentionally left unwired — `components/authz` has no per-`(table,id,identity)` read primitive today, so wiring it would mean guessing a permission convention; a composed-authz deployment still gates correctly via tokens in the meantime). `"public"`-visibility files instead resolve to a stable/CDN url with no token. An abandoned or deleted upload's row is reclaimed by a background reaper driver (`storageReaper`, built on the same recurring-driver seam as the scheduler) — deletes tombstone transactionally (immediate, reactive) while the actual bytes are reclaimed asynchronously on the next sweep, since byte I/O can't run inside the transactor. Range (`Range: bytes=`) requests are supported on the serve endpoint. Proven end-to-end through the real `stackbase serve` server in `packages/cli/test/storage-e2e.test.ts`: an FS-backend hermetic run (proxied upload → confirm → range-GET → reactive save into a user table) plus a real `minio/minio` container ship gate (presigned PUT straight to the bucket → confirm → signed-redirect GET → orphan-reap → delete-reclaim). See `docs/enduser/files.md`.) · **`@stackbase/triggers`** (component, opt-in per project via `stackbase.config.ts` alongside the scheduler — `examples/chat/stackbase.config.ts` shows the reference pattern, a `messages` audit-log trigger — `defineTriggers({table: {handler, batchSize?, fromStart?, maxDeliveriesPerWindow?}})` runs an internal (`_`-prefixed-path) mutation or action with `{changes: LogChange[]}` batches whenever a watched table's documents are inserted/updated/deleted, implemented as a durable CURSOR OVER THE MVCC LOG rather than a queue (built on a new `DriverContext.readLog` core seam with a fleet stable-prefix scan bound) so a missed change is impossible by construction; delivery is bounded at-least-once and in-order per document — a crash between handler success and cursor advance redelivers the last batch (possibly widened by new commits), but every change's `changeId = "<table>:<id>:<ts>"` is stable across redelivery for dedup — with a trigger pausing itself after 8 consecutive handler failures (`triggers:resume` to un-pause, picked up within ~30s) and a 1000-deliveries/10s circuit breaker so a trigger legitimately writing its own watched table (DB-trigger semantics — delivered, not excluded) can't melt the node; new triggers start at the log's current tip unless `fromStart: true` replays full history, proven end-to-end through the real `stackbase dev` server in `packages/cli/test/triggers-e2e.test.ts` (reactive mutation-handler fan-out, an action handler, crash-resume with a genuine undelivered backlog, and the recursion breaker). See `docs/enduser/triggers.md`.) · **client optimistic updates** (`useMutation(api.x.y).withOptimisticUpdate((store, args) => …)` — Convex-verbatim: instant, synchronous local UI on mutation call, exact rollback (stop-replaying, never inverse ops), stacking, and a no-flicker reconciliation contract — the frame an optimistic layer disappears is always the same frame the authoritative row appears (drop-on-observed-inclusion, never drop-on-ack), pinned by the "Gated Ledger" design in `docs/dev/research/client-sync/verdict.md`. `packages/client`'s new `S1 MutationLog`/`S2 LayeredQueryStore`/`S3` reconcile chokepoint/`S4 DeliveryPolicy` back it; deterministic `store.placeholderId(table)`/`store.now()` (never `crypto.randomUUID()`/`Date.now()` inside an updater — replay purity is enforced by API shape) mint stable temp ids/timestamps across the replay-on-every-ingest rebuild; dev-mode `Object.freeze` (shallow) on every value the store hands back catches in-place mutation; typed `OptimisticLocalStore` (`getQuery`/`setQuery`/`getAllQueries`) is driven by a function's `returns` validator, the same D10 codegen mechanism `argsType` already used — return-type codegen shipped as this slice's prerequisite. Promises resolve at commit (`MutationResponse`), an explicit divergence from convex-js's gate-time resolution, documented as a migration note. Four server/protocol repairs shipped alongside it: `MutationResponse` now carries `ts` (with a `commitTs > 0` send-site assertion), the **G4 origin-frontier guarantee** (an ephemeral `origin` tag threaded through the commit fan-out — parallel to, never through, `commitMeta` — so a session's own commit always advances its observed frontier before or exactly when it's told about it, proven on both SQLite and `docstore-postgres`), **G1 serialization** (subscribe-response processing enqueued onto the per-session notify tail, closing a base-regression race), and undroppable `MutationResponse`/`ActionResponse` under backpressure. The WebSocket transport now **reconnects by default** (exponential backoff + jitter, `{reconnect: false}` opt-out) — on reopen it replays `SetAuth`, resubscribes every live query, and FIFO-flushes only `unsent` mutations (an `inflight` mutation whose outcome is unknowable rejects with a typed `MutationUndeliveredError` instead of being blindly resent). Proven end-to-end through a real WebSocket `stackbase dev` server in `packages/cli/test/optimistic-e2e.test.ts`, including response-before-Transition wire-order, reconnect-kill-resubscribe-flush, the backpressure exemption, the G4 adapter-timing proof on both docstores, and a concurrent cross-shard no-flicker test that **found and fixed a real bug**: `ShardedTransactor` was fanning a commit's timestamp to the other shard oracles only in a post-resolve step, after publish had already fired, so a cross-shard query could re-run on a stale snapshot and show a one-frame flicker under the 8-shard default — fixed by an `onCommitted` hook firing between a shard's own `publishCommitted` and its `fanout.publish`. `examples/chat` demonstrates it (`web/main.tsx`); see `docs/enduser/optimistic-updates.md` for the API, the purity rules, temp-id constraints (a `placeholderId()` is a deterministic-but-non-decodable string, never a real `Id<>` — never pass one as a mutation argument), the promise-timing migration note, the two documented residuals (echo-snap on a wrong guess; `QueryFailed`-on-confirm), the shallow-freeze caveat, and the pending-row type-widening recipe (no first-class `pending` metadata channel in v1). A **durable offline mutation outbox** — reload/crash survival, safe resend of sent-but-unacknowledged mutations — is the deliberate, committed next slice, not built here; see the deferred note below.).

Honestly deferred (seams reserved, not built): true V8-isolate global sandboxing (the inline executor runs in-process; the syscall ABI is isolate-ready) · a durable client-side offline mutation outbox (the client's committed next slice — S1's serializable `(requestId, udfPath, args)` triple, S4's policy split, and the deliberately-opaque `requestId` were all left open specifically to receive it; server bill = per-client dedup atomic with commit, poison-pill semantics, session resumption; research input already banked: `docs/dev/research/client-sync/verdict.md` §(g)'s receiving-seam table, and `docs/dev/research/lunora.md` §5's `clientId + monotone clientSeq` watermark-reconciliation design, flagged by the user as the closest competitor's shipped answer to the same problem) · `maxObservedTimestamp` fast-resume / full session-resumption on reconnect (today's reconnect re-subscribes and re-sends full query results rather than resuming from a watermark — fine at today's scale) · Tier 2 distributed sharding/sync fleet (multi-node write scale-out — the Postgres adapter's single-writer advisory lock is single-node durability, not this) · search/vector · a native `Bun.SQL` fast-follow for the Postgres adapter (the `pg` driver is what shipped) · TLS termination (Stackbase serves plain HTTP; front it with nginx/Caddy/Traefik) · per-user/per-role file-read authorization (the `checkRead` seam on the storage serve endpoint is threaded but unwired — see file storage above). (Range-precise invalidation has since SHIPPED — the recorded read/write ranges now drive subscription invalidation, so a write only re-runs subscriptions whose read-set its range intersects; activated by the authz effectivePermissions slice. Scheduled functions/crons have since SHIPPED too — see `@stackbase/scheduler` above. Actions have since SHIPPED too — see above. Durable multi-step workflows have since SHIPPED too — see `@stackbase/workflow` above, including saga/compensation; sub-workflows, replay-debugging, and handler versioning remain deferred within that slice, see its own note. `httpAction` + the public HTTP router have since SHIPPED too — see above. `stackbase serve` + working Docker self-host have since SHIPPED too — see above. `stackbase deploy` push-based live deploy (slice 6b) has since SHIPPED too — see above. The single-binary compile (`stackbase build`) has since SHIPPED too — see above. The Postgres adapter (slice 6c) has since SHIPPED too — see above. File storage (slice 4) has since SHIPPED too — see above; only multi-node (Tier 2) remains unbuilt among the originally-deferred items. Client optimistic updates have since SHIPPED too — see above; the durable offline mutation outbox that was bundled with it in the original "optimistic updates + full version-gap resync" phrasing is now its own, still-deferred line item above, since it's the deliberately committed next slice rather than an open-ended someday.)

## Locked decisions (do not relitigate without the user)

- **Language:** Full TypeScript end-to-end — CLI, server engine, and client SDK are one language. No Rust core. Optimize for a small team shipping a *complete, working* system.
- **Storage is pluggable.** The engine must NOT hard-depend on one database. All persistence goes through a `DatabaseAdapter` interface; ship adapters for **SQLite** (zero-config local dev / single-node) and **Postgres** (scalable deploy), and keep the door open for others. Engine logic never imports a driver directly.
- **Runtime:** **Bun is primary** (server, dev, and the single binary via `bun build --compile`); the engine is runtime-agnostic behind `DatabaseAdapter`/runtime seams, and **Node is fully supported** (npm packages + `NodeSqliteAdapter`). Tests run under Node/vitest (runtime-neutral logic); the Bun path has a `bun run` smoke test in `docstore-sqlite`.
- **Deployment baseline:** Docker self-host. `docker compose up` brings up engine + database + dashboard in one container. Portability over any single cloud.
- **Reference, not dependency:** study Convex (`get-convex/convex-backend`, `get-convex/convex-js`) for protocol/DX shape. Do not copy FSL-licensed code; reimplement.
- **Product identity: native `@stackbase/*`; Convex-compat is a migration on-ramp, NOT the canonical surface.** Stackbase is its own BaaS, not a Convex drop-in — it has already grown past Convex (components, durable workflows + saga, Postgres adapter, file storage, single-binary build). The canonical public API and imports are `@stackbase/*` (e.g. `import { v } from "@stackbase/values"`); the `convex/` app-directory name is kept only as a low-friction default, not a compatibility promise. Convex/Supabase/Firebase users arrive via **`stackbase migrate`** (Convex first — near-free, mostly an import codemod since the runtime is already Convex-shaped; other sources are pluggable follow-ons behind a source-adapter seam). An optional *local* compat alias (e.g. tsconfig/bundler `convex/*` → `@stackbase/*`) is acceptable as a zero-change-lift convenience, but never the documented canonical surface (you cannot publish a package named `convex` anyway). Decided 2025-08-12, **superseding** the earlier "Convex-compat surface preserved" docs stance below.
- **Business model & licensing: free now, gate scale later — full detail in [`docs/dev/business-model-and-licensing.md`](docs/dev/business-model-and-licensing.md).** Everything ships **free now (including scale, when built)** to win adoption; **later, after popularity, a paid license key unlocks scaling up** (distributed Tier 2 + enterprise) — you still **deploy anywhere on your own infra; the key unlocks a capability, not deployment**. The **n8n / GitLab-EE model** (sell keys, *not* compute — no mandatory managed cloud near-term). The one non-negotiable: set the protective **license now**, add the **paywall code later** — never the reverse. → License is **FSL-1.1-Apache-2.0** (free to use/self-host, forbids offering Stackbase as a competing service, converts to Apache after 2yr — the license Convex uses) from the first public commit; future paid code lives in a reserved **`ee/`** area under a *separate, non-converting commercial license*; **CLA/DCO required from day one** to preserve relicensing rights; build the `license.has("scale")` **entitlement seam alongside Tier 2**. Never start MIT/permissive-then-relicense (the Redis/Elastic/HashiCorp/Terraform rug-pull-and-fork trap); "free" ≠ "MIT". Single-node self-host, deploy-anywhere, and data/code portability are **free forever**. Decided 2025-08-12.

## Build order (each is its own working slice, spec'd and built independently)

1. **Foundation** ✅ — reactive engine: schema + query/mutation function definitions, transactional execution via the DB adapter, WebSocket reactive subscriptions, the `stackbase dev` CLI that pushes functions, and a client SDK with a `useQuery` hook.
2. **Dashboard** ✅ — `apps/dashboard`: live data browser (admin sync subscription, cursor pagination, structured filters, scanCapped banner), logs viewer, function runner. Table list via HTTP (lazy + manual refresh); doc edits via admin HTTP (live subscription reflects writes).
3. Auth
4. File storage — **✅ shipped**: `BlobStore` seam + `@stackbase/blobstore-fs`/`@stackbase/blobstore-s3` adapters, `_storage` system table + `ctx.storage`, two-phase proxied/presigned uploads, private-by-default bearer-token serving, orphan reaper (see What works above)
5. Actions + scheduled functions / crons (side effects that run *outside* the transaction) — **all three parts ✅ shipped**: scheduled functions/crons (`@stackbase/scheduler`), actions themselves, and `httpAction` + the public HTTP router (webhooks) (see What works above)
6. Production deploy tooling — **slice 6a (Docker self-host) ✅ shipped**: `stackbase serve` + working `docker compose up`. **Slice 6b (`stackbase deploy` push-based live hot-swap) ✅ shipped**: opt-in `--allow-deploy`, additive-only schema with destructive-reject, atomic swap, E2E through the real server (see What works above). **Single-binary compile (`stackbase build`) ✅ shipped**: `bun build --compile` self-contained executable, cross-compile targets, `{"ready":…}` startup line, E2E through a real compiled binary (see What works above). **6c (Postgres adapter) ✅ shipped**: `@stackbase/docstore-postgres` on the `pg` driver, selectable via `--database-url`/`STACKBASE_DATABASE_URL`, single-writer advisory lock, no app-schema migrations, SQLite+PGlite conformance parity plus a real-container Docker E2E (see What works above).

Do not start a later slice before the earlier one runs end-to-end.

## Canonical architecture reference

Before designing or building any slice, read **`docs/dev/architecture/system-design.md`** (the North Star), **`docs/dev/architecture/internals/README.md`** (ground-truth engine internals), and **`docs/dev/research/comparison.md`**. They define the reactive-transaction core, the storage seam, and the **tiered architecture** (single-binary Tier 0 → distributed Tier 2, same app code throughout) that reconciles the four goals: Convex-DX, PocketBase-lightweight, SpacetimeDB-fast, concave-deploy-anywhere. Per-system research is in `docs/dev/research/`.

The `docs/dev/architecture/internals/` notes are **clean-room extractions** from the published `@concavejs/*` packages (studied as reference; the packages are FSL-1.1-Apache-2.0 and live in gitignored `.reference/` — **never copy that code into our packages**, see `.reference/README.md`). Key decisions already locked from the extraction: data model is an append-only **MVCC log** (`{ts, id, value, prev_ts}`); invalidation was **table-level first, now range-precise** (recorded read/write ranges drive intersection — activated by the authz effectivePermissions slice); **3-phase OCC** with deterministic-UDF replay on conflict; syscall ABI must be **fully serializable across a V8 isolate** from day one.

## The one concept to get right: the reactivity model

This is what makes the system "Convex-like" and is the easiest thing to design wrong.

- **Queries are pure, deterministic, read-only.** During execution, the engine records the query's **read set** — exactly which tables/rows/index-ranges it touched.
- **Mutations are the only writers**, and they run as a single serializable transaction. On commit, the engine computes the **write set**.
- A subscribed query is **re-run and re-pushed** only when a committed write set **intersects** its recorded read set. No polling. This intersection logic — not the WebSocket plumbing — is the heart of the system. Get the read-set granularity and invalidation right and everything else follows.
- Because invalidation depends on deterministic re-execution, **queries must not call non-deterministic APIs** (network, random, clock). Side effects belong in *actions* (slice 5), never in queries/mutations.

## Intended architecture (target monorepo layout)

TypeScript monorepo (Bun workspaces + Turborepo). Keep packages small and single-purpose — the engine should be understandable without reading the CLI, and vice versa. Top-level dirs: `packages/` (engine + SDK), `components/` (pluggable components, e.g. `@stackbase/auth`), `apps/` (dashboard), `examples/`.

- `packages/server` — the engine: function registry, transaction manager, read/write-set tracking, subscription invalidation, WebSocket sync server.
- `packages/adapters/*` — `DatabaseAdapter` implementations (`sqlite`, `postgres`). One package per backend; engine depends only on the interface.
- `packages/client` — framework-agnostic client + sync protocol; `packages/react` (or a subpath) for `useQuery`/`useMutation` hooks.
- `packages/cli` — `stackbase` CLI: `dev` (watch + push functions + run engine), `deploy`, codegen.
- `packages/codegen` — generates typed API + schema types consumed by app code (the typed-client DX is a core selling point; treat it as load-bearing, not a nicety).
- `apps/dashboard` — live data browser, logs, and function runner (slice 2, shipped).
- `examples/*` — runnable sample apps that double as integration tests.

## Commands (target — confirm they exist before use)

```bash
bun install               # bootstrap workspace (Bun is the package manager + runtime)
bun run build             # build all packages (Turborepo, topological)
bun run dev               # watch-build packages
bun run test              # run all tests (vitest, under Bun)
bun run --filter <pkg> test    # single package's tests (e.g. --filter @stackbase/auth)
bun run lint && bun run typecheck

# end-user-facing CLI (what the DX is judged on):
stackbase dev             # local: watch functions, push to engine, serve sync
stackbase serve           # production: required admin key, 0.0.0.0, no codegen, graceful shutdown
docker compose up         # self-host: generic image + bind-mounted convex/ + persistent SQLite volume
```

When you add the first package, also add the script wiring so these top-level commands actually work — DX is the product here, so the commands must be real, not aspirational.

## Working conventions

- **Two doc audiences, kept separate.** `docs/enduser/` is the **public, end-user product documentation** (how a developer *uses* Stackbase — this is the eventual `docs.stackbase.dev` site; derived from concave's docs, rebranded — canonical imports are `@stackbase/*` (NOT `convex/*`), per the "Product identity" locked decision above; Convex is referenced only in a "migrate from Convex" framing, and the stale `convex/*` import pages are being swept to `@stackbase/*`). Internal engineering docs (specs, architecture decisions) live separately under `docs/superpowers/specs/` (or `docs/internal/`). Do not mix them. Raw upstream reference is kept at `.reference/concave-docs-raw/`.
- **Process:** this project uses the brainstorming → writing-plans → implementation flow. Each build-order slice gets a design spec in `docs/superpowers/specs/` before code. Don't skip to coding a slice without an approved spec.
- **DX is the feature.** Error messages from the CLI/SDK, type inference quality, and `stackbase dev` startup speed are not polish — they are the reason to choose this over alternatives. Weigh changes against them.
- **Never let the engine know which database it's on.** A leak of SQLite/Postgres specifics out of `packages/adapters/*` is a design bug.
