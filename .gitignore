# Reference material only — third-party sources studied for understanding.
# NEVER ship or copy this into our own packages (see .reference/README.md).
.reference/

# deps / build
node_modules/
dist/
.turbo/
*.tsbuildinfo

# env / local
.env
.env.*
!.env.example
.DS_Store

# helipod local data (Tier 0 embedded db)
*.db
*.db-wal
*.db-shm
.helipod/
.helipod-deploy/

# benchmark results
benchmarks/results/

# generated example web bundle (run `pnpm web:build`)
examples/*/web/main.js
examples/*/web/*.js.map

# built dashboard SPA bundle (run `pnpm --filter @helipod/dashboard build`)
apps/*/dist/

# local agent / tooling / verification artifacts
.playwright-mcp/
.workflows/
.remember/
.claude/
helipod-chat-working.png

# SDD scratch (progress ledger, briefs, reports)
.superpowers/

# local-only (excluded from public repo)
/benchmarks/
/docs_old/
