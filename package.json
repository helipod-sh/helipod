{
  "name": "stackbase",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Stackbase \u2014 a Convex-compatible, self-hostable reactive backend. Lightweight by default, scalable on demand.",
  "license": "MIT",
  "packageManager": "bun@1.3.11",
  "engines": {
    "node": ">=22",
    "bun": ">=1.2"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "components/*",
      "examples/*",
      "apps/*",
      "ee/packages/*",
      "benchmarks/convex-comparison",
      "benchmarks/runner"
    ],
    "catalog": {
      "typescript": "^5.7.2",
      "tsup": "^8.3.5",
      "vitest": "^2.1.8",
      "@types/node": "^22.10.5",
      "@aws-sdk/client-s3": "^3.1079.0",
      "@aws-sdk/s3-request-presigner": "^3.1079.0"
    }
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules",
    "bench:reactive": "bun benchmarks/runner/src/cli.ts run --axis reactive",
    "bench:writes": "bun benchmarks/runner/src/cli.ts run --axis writes",
    "bench:sharded": "bun benchmarks/runner/src/cli.ts run --axis sharded --store pg",
    "bench:compare": "bun benchmarks/runner/src/cli.ts compare",
    "bench:objectstore": "bun benchmarks/runner/src/objectstore-bench.ts",
    "bench:pgbench": "bun benchmarks/runner/src/pgbench-baseline.ts",
    "bench:wswrite": "bun benchmarks/runner/src/ws-write-bench.ts",
    "bench:broadcast": "bun benchmarks/runner/src/broadcast-dedup-bench.ts"
  },
  "trustedDependencies": [
    "@embedded-postgres/darwin-arm64",
    "@embedded-postgres/darwin-x64",
    "@embedded-postgres/linux-arm64",
    "@embedded-postgres/linux-x64",
    "@embedded-postgres/windows-x64"
  ],
  "devDependencies": {
    "embedded-postgres": "16.14.0-beta.17",
    "turbo": "^2.3.3",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
