---
"@helipod/tui": patch
---

The overview gains a chart row: calls over time as a line chart, a latency
histogram with fixed buckets, and a success-rate gauge — all from the engine's
execution log, refreshed on every commit.
